import ProductsPage from '../../components/ProductsPage';

export const metadata = {
  title: 'Products | Strain Clamp, Parallel Groove Clamp, Safety Sign, Safety Helmet',
  description: 'Selected power line hardware and electrical safety products for Vietnam market: strain clamp, parallel groove clamp, insulation piercing grounding clamp, signs and helmets.',
  keywords: 'power line hardware, strain clamp, parallel groove clamp, insulation piercing clamp, electrical safety sign, safety helmet, Vietnam power project'
};

export default function Page() {
  return <ProductsPage />;
}
